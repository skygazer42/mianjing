# 24. 为什么空格、标点、缩进、换行在 tokenizer 里也很重要？

## 回答

这些符号不是无意义的分隔符。自然语言中，空格会区分词边界，标点会影响句法、语气和列表结构；不少 tokenizer 还会把前导空格与后续英文词合并为一个 token。

在代码和结构化文本中，换行、缩进、逗号、引号和括号直接决定语法与层级。Python 的缩进、JSON 的引号、YAML 的空格都不能被随意丢弃或规范化。

## 面试要点

处理代码、提示词模板、Markdown、JSON 或工具调用时，要用目标 tokenizer 实测，并避免预处理阶段无意删除空格和换行。
