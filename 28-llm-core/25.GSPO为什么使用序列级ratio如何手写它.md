# 25. GSPO 为什么使用序列级 ratio，如何手写它？

    ## 结论先行

    GSPO 的直觉是把一条完整回答作为策略更新与裁剪的基本单位：先汇总该回答的 log-prob 变化得到序列级 ratio，再结合组内相对优势做 clip surrogate。这样避免把同一回答拆成许多 token 后，长序列因 token 数更多而获得不同的隐式权重。实现时必须明确采用 sum 还是长度归一化 log-prob，并记录 ratio 分布、clip fraction 和组奖励。实验对两道客服处理 prompt 各采样三条完整回复，比较未归一化序列 ratio 与按长度平均的序列 ratio。

    ## 公式与具体例子

    一种序列级写法为 $r_i=\exp((\log\pi_\theta(y_i|x)-\log\pi_{old}(y_i|x))/|y_i|)$，$L=\min(r_iA_i,\operatorname{clip}(r_i,1\pm\epsilon)A_i)$。具体归一化约定必须固定。

    对应实验使用六条“退款、盗刷、登录、地址、冻结、发票”客服 prompt 或其流式键值映射，同一数据先跑 baseline，再输出核心状态、概率/ratio、结果表与真实失败修复。它是机制实验，不是线上收益结论。

    ## 工程取舍

    生产应冻结 rollout 版本、保存 old logprob/长度/reward，并用 ratio/clip/KL 分位数做报警；不同实现的 length normalization 不能混比。

    ## 常见坑

    用 sequence sum 却不说明长度口径，或将不同 prompt 的奖励直接混在一组里计算相对优势。

    ## 延伸追问

    序列 ratio 与 token ratio 在长 CoT 上为何不同？如何保证多轮更新时 old policy 不被错误覆盖？

    ## 原始论文 / 官方来源

    - [Group Sequence Policy Optimization](https://arxiv.org/abs/2507.18071)
- [PPO](https://arxiv.org/abs/1707.06347)

    ## 对应实验

    运行 `25-gspo-sequence-ratio.ipynb`。最后断言仅保护关键不变量，学习重点是输入、过程输出与失败修复。
