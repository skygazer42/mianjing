# 21. Delta Attention 如何用增量写入替代直接累加记忆？

    ## 结论先行

    Delta Attention/Delta Rule 的关键是写入新键值前先读取当前记忆对该 key 的预测，再只写入残差。这样同一 key 被新事实覆盖时，状态不会像简单 outer-product 累加那样无限叠加旧值。面试中要区分“固定状态大小”与“信息必然不丢”：状态固定只说明内存复杂度，遗忘与冲突要由更新规则处理。实验流式写入五条客服账户状态，其中同一账户状态被三次更新；比较 additive memory 与手写 delta write 的最新值误差和状态大小。

    ## 公式与具体例子

    给定状态 $S$、key $k$、value $v$，delta 写入可写为 $S\leftarrow S+\beta(v-Sk)k^T$；其中 $(v-Sk)$ 是当前预测残差。

    对应实验使用六条“退款、盗刷、登录、地址、冻结、发票”客服 prompt 或其流式键值映射，同一数据先跑 baseline，再输出核心状态、概率/ratio、结果表与真实失败修复。它是机制实验，不是线上收益结论。

    ## 工程取舍

    生产实现要处理多头、门控、归一化和长序列数值稳定性；固定 state 不能替代需要精确逐 token 回看的任务。

    ## 常见坑

    把 delta rule 写成单纯 $S+=vk^T$，会在 key 重复时保留过期事实并造成状态范数持续增长。

    ## 延伸追问

    如何给写入率 beta 加门控？key 非正交时，delta 更新为何可能互相干扰？

    ## 原始论文 / 官方来源

    - [Gated Delta Networks](https://arxiv.org/abs/2412.06464)
- [Linear Transformers Are Secretly Fast Weight Programmers](https://arxiv.org/abs/2006.16236)

    ## 对应实验

    运行 `21-delta-attention.ipynb`。最后断言仅保护关键不变量，学习重点是输入、过程输出与失败修复。
