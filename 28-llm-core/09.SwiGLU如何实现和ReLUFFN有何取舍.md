# 09. SwiGLU 如何实现，和 ReLU FFN 有何取舍？

    ## 结论先行

    SwiGLU 是门控前馈层：一条分支产生 value，另一条分支先做 SiLU 再充当 gate，二者逐元素相乘后投影回模型维度。相较单路 ReLU FFN，它多了一组投影参数，却让特征选择具有输入依赖的连续门控。面试时应写清 $\operatorname{SiLU}(a)=a\sigma(a)$，并说明为了参数预算公平，SwiGLU 的中间维度常会相应缩小。本实验手写 ReLU FFN 与 SwiGLU 分类器，观察训练 loss、准确率和 gate 激活；随后演示漏 SiLU 时尺度过大的失败。小数据只解释门控机制。

    ## 公式与具体例子

    $\operatorname{SwiGLU}(x)=(xW_g\odot\sigma(xW_g))\odot(xW_v)$，然后经 $W_o$ 投影。实现中常写为 $\operatorname{SiLU}(xW_g)\odot(xW_v)$。

    具体到“支付重复扣款、陌生转账、盗刷冻结、登录验证码、地址修改、发票更正”六条工单：先用同一特征矩阵建立最朴素基线，再记录本题机制所改变的更新、尺度、梯度或调度量。Notebook 会把输入、每一步中间量和同一指标下的对照直接打印出来；它是机制实验，不是线上 A/B 结论。

    ## 工程取舍

    SwiGLU 的两条输入投影提高参数和带宽成本；比较时应固定总参数/FLOPs，而不是给门控模型更多中间维度。

    ## 常见坑

    把 gate 写成裸乘法、忽略中间维度预算，或把训练集上的微小准确率差当成架构定论。

    ## 延伸追问

    为何 GLU 家族常用更小的 hidden width 才能与 ReLU FFN 参数公平？门控饱和时如何从激活分布和梯度诊断？

    ## 原始论文 / 官方来源

    - [GLU Variants Improve Transformer](https://arxiv.org/abs/2002.05202)
- [LLaMA](https://arxiv.org/abs/2302.13971)

    ## 对应实验

    运行同目录的 `09-swiglu.ipynb`。其中最后的少量断言只做回归保护；理解应以输入、对照、过程输出和失败修复为主。
