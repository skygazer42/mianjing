# 27. RLOO baseline 如何降低 REINFORCE 方差？

    ## 结论先行

    RLOO（REINFORCE Leave-One-Out）对同一 prompt 采样 K 条完整回复，对第 i 条用其余 K−1 条 reward 的均值作 baseline。这样 baseline 不含当前样本本身，能减少方差且不需要 critic。面试时要强调 baseline 应 stop-gradient，组必须来自同一 prompt，K=1 时 LOO 不成立。实验对两个客服 prompt 各采样三条回复，比较“包含自身的 group mean”与 leave-one-out advantage，并展示把不同 prompt 混组和 K=1 的失败。

    ## 公式与具体例子

    $A_i=r_i-\frac1{K-1}\sum_{j\ne i}r_j$；REINFORCE loss 可写为 $-A_i\sum_t\log\pi(y_{it}|x)$，优势和 baseline 不应反传梯度。

    对应实验使用六条“退款、盗刷、登录、地址、冻结、发票”客服 prompt 或其流式键值映射，同一数据先跑 baseline，再输出核心状态、概率/ratio、结果表与真实失败修复。它是机制实验，不是线上收益结论。

    ## 工程取舍

    生产要保存 prompt group id、rollout 版本和 reward 版本；同一 prompt 的候选数不齐时需要明确处理策略，而非静默填零。

    ## 常见坑

    用含自身的 group mean 当 LOO，或把不同 prompt、不同 reward rubric 的样本混在同一 baseline 里。

    ## 延伸追问

    RLOO 与 critic baseline 的方差/成本如何比较？K 增大为何既能稳优势又会增加 rollout 成本？

    ## 原始论文 / 官方来源

    - [Back to Basics: RLOO](https://arxiv.org/abs/2402.14740)
- [REINFORCE](https://link.springer.com/article/10.1007/BF00992696)

    ## 对应实验

    运行 `27-rloo-baseline.ipynb`。最后断言仅保护关键不变量，学习重点是输入、过程输出与失败修复。
