# 07. 深层 Transformer 为什么需要残差缩放和初始化策略？

    ## 结论先行

    深层残差网络的问题不是只有梯度消失，也包括每层新增分支的方差累积。一个常见思路是让残差分支初始化或输出尺度随深度衰减，例如乘以 $1/\sqrt{L}$，使总残差能量在深度增加时仍可控。它不是万能开关：缩放过强会减慢特征学习，缩放过弱会使激活和更新不稳定。面试时应同时说明初始化、残差系数、norm、学习率和深度是耦合配方。实验用 24 层手写残差 MLP 记录每层 RMS，比较未缩放与 $1/\sqrt{L}$ 缩放，并故意把残差系数设大制造失败。

    ## 公式与具体例子

    若每层残差近似独立且方差为 $\sigma^2$，直接相加后方差可随 $L$ 增长；令 $x_{l+1}=x_l+\alpha F_l(x_l)$ 且 $\alpha=1/\sqrt L$，可把累计尺度控制在常数量级。

    具体到“支付重复扣款、陌生转账、盗刷冻结、登录验证码、地址修改、发票更正”六条工单：先用同一特征矩阵建立最朴素基线，再记录本题机制所改变的更新、尺度、梯度或调度量。Notebook 会把输入、每一步中间量和同一指标下的对照直接打印出来；它是机制实验，不是线上 A/B 结论。

    ## 工程取舍

    生产模型要把缩放策略固化到配置和 checkpoint 兼容逻辑；深度、层类型和并行切分变化时必须重新验证，不应复用旧阈值。

    ## 常见坑

    只缩放初始化而忽略残差输出，或把每层都缩放到几乎没有学习信号，都会得到看似稳定但欠训练的网络。

    ## 延伸追问

    DeepNorm、ReZero、LayerScale 与 $1/\sqrt L$ 的假设差异是什么？MoE 层的 token 路由不均衡会如何改变残差尺度？

    ## 原始论文 / 官方来源

    - [DeepNet](https://arxiv.org/abs/2203.00555)
- [Fixup Initialization](https://arxiv.org/abs/1901.09321)

    ## 对应实验

    运行同目录的 `07-residual-scaling.ipynb`。其中最后的少量断言只做回归保护；理解应以输入、对照、过程输出和失败修复为主。
