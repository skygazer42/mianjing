# 02. μP 为什么能让不同宽度的超参数更可迁移？

    ## 结论先行

    μP（maximal update parameterization）讨论的是宽度变化时，初始化、学习率和参数尺度如何一起设定，使特征和函数更新保持可比。它不是“把所有层都除以宽度”的口号：输入层、隐藏层和读出层有不同的缩放规则。面试时应解释目标是最大更新而非单纯方差恒定，并说明要先在小宽度调参、再迁移到大宽度。本实验以工单风险分类为例，测量 8 宽与 64 宽网络在一次更新前后的 logit 漂移。数值接近只说明该受控初始化的一致性，不等价于大模型训练收益。

    ## 公式与具体例子

    两层网络可写为 $h=\phi(xW_1)$、$f=hW_2$。普通参数化常令 $W_2=O(1/\sqrt{n})$；μP 为保持宽度增大时函数更新量可控，会为不同参数张量配置不同的初始化和学习率尺度。

    具体到“支付重复扣款、陌生转账、盗刷冻结、登录验证码、地址修改、发票更正”六条工单：先用同一特征矩阵建立最朴素基线，再记录本题机制所改变的更新、尺度、梯度或调度量。Notebook 会把输入、每一步中间量和同一指标下的对照直接打印出来；它是机制实验，不是线上 A/B 结论。

    ## 工程取舍

    生产中需要明确 base shape、参数类别和 checkpoint 转换；只复制学习率而不复制 parametrization，迁移没有理论意义。

    ## 常见坑

    仅比较初始 loss，或把输出层与隐藏层用同一学习率缩放，都会掩盖真正的函数更新差异。

    ## 延伸追问

    如何在 attention 的 QKV、输出投影、embedding 和 MoE gate 上定义参数组？张量并行后 base shape 应按全局还是分片形状？

    ## 原始论文 / 官方来源

    - [Tensor Programs V：μP 原论文](https://arxiv.org/abs/2203.03466)
- [μP 参考实现](https://github.com/microsoft/mup)

    ## 对应实验

    运行同目录的 `02-mup.ipynb`。其中最后的少量断言只做回归保护；理解应以输入、对照、过程输出和失败修复为主。
