# 01. Muon 和 MuonClip 如何改变隐藏层矩阵更新？

## 结论先行

Muon 的核心不是把梯度逐元素缩放，而是先对隐藏层二维动量矩阵做近似正交化，再按矩阵谱结构更新。它适合矩阵形状的 hidden weights；embedding、标量、归一化参数通常仍由 AdamW 类更新。MuonClip 的重点是给正交化更新加全局或分层范数门限，防止某一批异常 token 把稳定方向放大成危险步长。面试中要同时说清方向和尺度：正交化负责方向，学习率、参数形状缩放和 clip 负责尺度。本教学实验把客服工单分类的线性隐藏矩阵拿出来，比较普通 SGD 与手写的正交动量更新。它只验证更新几何，不代表小数据上 Muon 会普遍优于 AdamW。

## 公式与具体例子

记动量为 $M_t=\beta M_{t-1}+(1-\beta)G_t$，Muon 用 $U_t\approx\operatorname{Orth}(M_t)$ 更新 $W_{t+1}=W_t-\eta U_t$；MuonClip 再令 $U_t\leftarrow U_t\min(1,c/(\lVert U_t\rVert_F+\epsilon))$。

具体到“支付重复扣款、陌生转账、盗刷冻结、登录验证码、地址修改、发票更正”六条工单：先用同一特征矩阵建立最朴素基线，再记录本题机制所改变的更新、尺度、梯度或调度量。Notebook 会把输入、每一步中间量和同一指标下的对照直接打印出来；它是机制实验，不是线上 A/B 结论。

## 工程取舍

生产中必须按参数类型分组，并记录正交化迭代残差、更新范数和 loss spike；矩阵形状、分布式分片与 BF16 数值误差都会改变实现。

## 常见坑

把所有一维参数也送入正交化，或只比较一个种子的一次 loss，就会把优化器的适用边界说错。

## 延伸追问

若 Q、K、V 被 fuse 成一个矩阵，正交化应该按 fuse 后矩阵还是按逻辑投影分块？如何用更新范数和验证损失决定 clip 阈值？

## 原始论文 / 官方来源

- [Muon 原论文](https://arxiv.org/abs/2502.16982)

## 对应实验

运行同目录的 `01-muon.ipynb`。其中最后的少量断言只做回归保护；理解应以输入、对照、过程输出和失败修复为主。
