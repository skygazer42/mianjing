# 08. RMSNorm 是什么，为什么 LLM 常用它？

## 结论先行

RMSNorm 用向量的均方根做缩放，不像 LayerNorm 那样显式减均值。它保留重缩放不变性、实现更简洁，并在很多 Transformer 配方中是有效选择；但它不自动保证“均值为零”，是否替代 LayerNorm 应由完整训练配方验证。面试中要写清公式、epsilon 的数值作用和可学习 scale，并解释 fused kernel 与低精度累积。实验用 6 条工单的隐藏表示比较未归一化与手写 RMSNorm 的每样本 RMS，并以全零向量演示漏 epsilon 导致 NaN。

## 公式与具体例子

$\operatorname{RMSNorm}(x)=g\odot x/\sqrt{\frac{1}{d}\sum_{j=1}^{d}x_j^2+\epsilon}$。它不减去 $\operatorname{mean}(x)$，因此与 LayerNorm 的平移不变性不同。

具体到“支付重复扣款、陌生转账、盗刷冻结、登录验证码、地址修改、发票更正”六条工单：先用同一特征矩阵建立最朴素基线，再记录本题机制所改变的更新、尺度、梯度或调度量。Notebook 会把输入、每一步中间量和同一指标下的对照直接打印出来；它是机制实验，不是线上 A/B 结论。

## 工程取舍

生产上应在 FP32 中累积平方和、在 BF16/FP16 中谨慎设置 epsilon，并验证 fused 实现与训练 checkpoint 的参数命名和精度一致。

## 常见坑

省略 epsilon、把 RMSNorm 说成“完全等价于 LayerNorm”，或者把跨 token 的 RMS 当成逐 token RMS。

## 延伸追问

RMSNorm 没有去均值会在哪类分布偏移下更敏感？partial RMSNorm 如何用子维度近似同时控制误差？

## 原始论文 / 官方来源

- [RMSNorm 原论文](https://arxiv.org/abs/1910.07467)

## 对应实验

运行同目录的 `08-rmsnorm.ipynb`。其中最后的少量断言只做回归保护；理解应以输入、对照、过程输出和失败修复为主。
