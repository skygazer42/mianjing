# 06. Pre-LN 与 Post-LN Transformer 有什么训练差异？

    ## 结论先行

    Pre-LN 在子层之前做归一化，残差支路是更直接的恒等路径；Post-LN 则在残差相加后归一化。两者并非谁绝对更强：Post-LN 在某些配方下可达到很高质量，但深层训练更依赖 warmup、初始化和残差尺度；Pre-LN 往往更容易从头稳定优化。面试回答必须区分前向表达与反向路径，并强调最终归一化、学习率和深度会改变结论。本实验手写小型残差堆叠，测量首层梯度范数和无归一化时的激活漂移。它是结构诊断，不是语言建模基准。

    ## 公式与具体例子

    Pre-LN：$x_{l+1}=x_l+F(\operatorname{LN}(x_l))$；Post-LN：$x_{l+1}=\operatorname{LN}(x_l+F(x_l))$。Pre-LN 的残差恒等支路使梯度可直接跨层传播。

    具体到“支付重复扣款、陌生转账、盗刷冻结、登录验证码、地址修改、发票更正”六条工单：先用同一特征矩阵建立最朴素基线，再记录本题机制所改变的更新、尺度、梯度或调度量。Notebook 会把输入、每一步中间量和同一指标下的对照直接打印出来；它是机制实验，不是线上 A/B 结论。

    ## 工程取舍

    生产需统一 checkpoint 中 norm 的位置、最终 norm、残差分支精度和 fused kernel；不能只替换一行 LayerNorm 就加载旧权重。

    ## 常见坑

    用浅层单 batch 的 loss 宣称某种 norm 一定更好，或者漏掉 final norm 与 warmup 的联合作用。

    ## 延伸追问

    为什么深度增加时 Post-LN 常需要更谨慎的 warmup？RMSNorm 替代 LayerNorm 后，Pre/Post 的稳定性假设有哪些变化？

    ## 原始论文 / 官方来源

    - [On Layer Normalization in the Transformer Architecture](https://arxiv.org/abs/2002.04745)
- [DeepNet](https://arxiv.org/abs/2203.00555)

    ## 对应实验

    运行同目录的 `06-pre-ln-post-ln.ipynb`。其中最后的少量断言只做回归保护；理解应以输入、对照、过程输出和失败修复为主。
