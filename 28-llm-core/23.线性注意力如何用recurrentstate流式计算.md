# 23. 线性注意力如何用 recurrent state 流式计算？

    ## 结论先行

    线性注意力通过可分解核特征 $\phi(q)^T\phi(k)$，把历史 key/value 汇总为累积状态，从而以常数每 token 代价递推。典型状态是 $S_t=S_{t-1}+\phi(k_t)v_t^T$ 与 $z_t=z_{t-1}+\phi(k_t)$，输出为 $\phi(q_t)^TS_t/(\phi(q_t)^Tz_t)$。面试中必须指出这通常是 softmax attention 的近似，不应声称完全等价。实验对五个流式客服 token 手写 causal softmax 基线和 ELU+1 特征映射的 recurrent state，打印逐 token 输出及近似误差；再展示漏分母导致尺度随长度增长。

    ## 公式与具体例子

    $S_t=\sum_{i\le t}\phi(k_i)v_i^T,\ z_t=\sum_{i\le t}\phi(k_i),\ o_t=\phi(q_t)^TS_t/(\phi(q_t)^Tz_t+\epsilon)$。

    对应实验使用六条“退款、盗刷、登录、地址、冻结、发票”客服 prompt 或其流式键值映射，同一数据先跑 baseline，再输出核心状态、概率/ratio、结果表与真实失败修复。它是机制实验，不是线上收益结论。

    ## 工程取舍

    生产需要选稳定正特征映射、控制 state 精度和 chunk 边界；复杂检索任务常仍需部分 softmax attention 或外部检索。

    ## 常见坑

    忘记归一化分母、把线性注意力称为精确 softmax、或 chunk 时重复累计边界 token。

    ## 延伸追问

    正特征映射如何影响近似质量？为什么 state 必须同时保存矩阵 S 和向量 z？

    ## 原始论文 / 官方来源

    - [Transformers are RNNs](https://arxiv.org/abs/2006.16236)
- [Rethinking Attention with Performers](https://arxiv.org/abs/2009.14794)

    ## 对应实验

    运行 `23-linear-attention-state.ipynb`。最后断言仅保护关键不变量，学习重点是输入、过程输出与失败修复。
