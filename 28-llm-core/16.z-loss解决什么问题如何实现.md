# 16. z-loss 解决什么问题，如何实现？

    ## 结论先行

    z-loss 在交叉熵之外惩罚 $z=\log\sum\exp(\text{logits})$ 的绝对大小，常用于抑制 softmax partition 的无界漂移、改善大规模低精度训练稳定性。它不是让所有 logits 变小，更不是替代准确率目标；系数过大可能压低置信度、伤害主任务。面试回答要区分 logit shift 不变的交叉熵与 z-loss 对绝对 partition 的约束。实验用六条意图分类 logits 比较纯 CE 与 CE+z-loss 的梯度，并构造过大系数导致主损失被压制的失败。

    ## 公式与具体例子

    $L=L_{CE}+\lambda_z(\log\sum_j\exp z_j)^2$。交叉熵对同一行 logits 加常数近似不变，但 z-loss 会惩罚这种整体上移。

    对应 Notebook 使用“支付退款、登录验证码、陌生转账、地址修改、盗刷冻结、发票更正”六条脱敏客服事件，先给出同口径 baseline，再打印核心实现的中间量、结果表和可复现失败。受控小数据只用于讲清机制，不可外推为生产收益。

    ## 工程取舍

    生产中应记录 CE、z-loss、logsumexp 分布和溢出计数，并按模型规模、精度和数据配方调小系数；不能只报总 loss。

    ## 常见坑

    把 logsumexp 当成 logits 均值，或给过大的 z-loss 系数导致模型为了减 z 而牺牲分类/生成质量。

    ## 延伸追问

    z-loss 与 logit clipping、RMSNorm、BF16 overflow 有何关系？为何需要分别记录总 loss 和各分项？

    ## 原始论文 / 官方来源

    - [PaLM：z-loss 描述](https://arxiv.org/abs/2204.02311)
- [T5X z-loss 实现](https://github.com/google-research/t5x)

    ## 对应实验

    运行 `16-z-loss.ipynb`。Notebook 最后仅保留少量断言用于回归保护，学习重点是可见输入、中间量、对照和失败修复。
