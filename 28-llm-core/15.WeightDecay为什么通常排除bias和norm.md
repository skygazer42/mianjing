# 15. Weight Decay 为什么通常排除 bias 和 norm？

    ## 结论先行

    AdamW 式 decoupled weight decay 通常施加在矩阵权重上，而 bias 与 LayerNorm/RMSNorm 的 scale/bias 经常被排除。理由不是“它们永远不能正则化”，而是这些参数维度小、语义是平移/缩放，统一衰减可能直接破坏标定。面试时要区分 L2 penalty 与 decoupled decay，并说明参数名匹配需要审计。实验对分类矩阵、bias 和 RMS scale 连续施加衰减，比较全量 decay 与只 decay 矩阵；随后展示用模糊名称规则误伤 norm 的失败。

    ## 公式与具体例子

    AdamW 的简化更新是 $\theta\leftarrow(1-\eta\lambda)\theta-\eta u$。是否衰减应由参数角色决定，而不只由张量维度决定。

    对应 Notebook 使用“支付退款、登录验证码、陌生转账、地址修改、盗刷冻结、发票更正”六条脱敏客服事件，先给出同口径 baseline，再打印核心实现的中间量、结果表和可复现失败。受控小数据只用于讲清机制，不可外推为生产收益。

    ## 工程取舍

    生产中应按 module/type 建组而不是脆弱的字符串规则，并在启动时打印 decay/no-decay 参数数和名称抽样。

    ## 常见坑

    把所有一维张量排除会漏掉某些真正权重；按名字含 norm 判断又可能漏掉自定义 RMSNorm。

    ## 延伸追问

    LoRA A/B 矩阵是否应 decay？embedding、输出头和 adapter 的 decay 如何通过验证损失与权重范数共同调参？

    ## 原始论文 / 官方来源

    - [Decoupled Weight Decay Regularization](https://arxiv.org/abs/1711.05101)
- [PyTorch AdamW](https://pytorch.org/docs/stable/generated/torch.optim.AdamW.html)

    ## 对应实验

    运行 `15-weight-decay-exclusions.ipynb`。Notebook 最后仅保留少量断言用于回归保护，学习重点是可见输入、中间量、对照和失败修复。
